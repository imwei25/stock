# Tier 1 性能优化 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 backtest / portfolio-backtest / portfolio-ab 三条路径上落地 4 项独立的性能优化,每项都有 minimal example 验证优化前后输出在容差内一致。

**Architecture:** 4 个独立 PR,顺序:#4 preprocess 批量 OLS → #1 Rust ops PR-4 → #3 StaggeredRunner 并行 → #2 portfolio_ab 两 arm 并行。每个 PR 都可独立 revert,各自有 minimal 校验脚本/测试。

**Tech Stack:** Python 3.10 (`.venv/Scripts/python.exe`)、pytest、PyO3 0.22 + Rust(maturin build)、numpy/pandas、`concurrent.futures.ProcessPoolExecutor`。

**Spec:** `docs/superpowers/specs/2026-06-21-perf-tier1-design.md`

---

## PR-T1.1 — `industry_neutralize_panel(log_mcap=...)` 批量 lstsq

预算:1 天 | 风险:低(纯数学;mcap_neutralize_panel 已有同范式)

**文件结构:**
- Modify: `src/stockpool/ml/preprocess.py:201-274` — 把 per-day loop 改成批量 normal equation,并保留旧实现作为 oracle 备份
- Modify: `tests/test_ml_preprocess_mcap.py` — 新增对比测试

### Task 1.1: 保留旧 per-day 路径作为 oracle

**Files:**
- Modify: `src/stockpool/ml/preprocess.py:201-274`

- [ ] **Step 1: 把旧实现重命名为 `_industry_neutralize_per_day_loop` 并留在文件里(test-only)**

把现有 `industry_neutralize_panel` 函数原封不动复制成下面这个新私有函数,放在原函数下方:

```python
def _industry_neutralize_per_day_loop(
    df: pd.DataFrame,
    sector_map: Mapping[str, str],
    log_mcap: pd.DataFrame | None = None,
) -> pd.DataFrame:
    """LEGACY per-day OLS loop. Kept ONLY as a test oracle (see
    tests/test_ml_preprocess_mcap.py::test_industry_log_mcap_batch_matches_legacy).
    Not used in production after PR-T1.1."""
    # ... 当前函数体完整复制 ...
```

不要改名原 `industry_neutralize_panel` — 它在 Task 1.3 才会被替换。

- [ ] **Step 2: 运行现有测试确认行为不变**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ml_preprocess_mcap.py tests/test_ml_preprocess.py -q`
Expected: PASS(只是新增了死代码,不影响行为)

- [ ] **Step 3: Commit**

```bash
git add src/stockpool/ml/preprocess.py
git commit -m "refactor(preprocess): preserve per-day loop as oracle (no-op)"
```

### Task 1.2: 写失败的对比测试

**Files:**
- Modify: `tests/test_ml_preprocess_mcap.py` — 在文件末尾追加

- [ ] **Step 1: 添加 import 和测试**

```python
import numpy as np
import pandas as pd
from stockpool.ml.preprocess import (
    industry_neutralize_panel,
    _industry_neutralize_per_day_loop,
)


def test_industry_log_mcap_batch_matches_legacy():
    """新批量路径 vs 旧 per-day loop 在合成 panel 上 bit-near-exact."""
    rng = np.random.default_rng(42)
    T, N = 50, 200
    n_industries = 5
    dates = pd.date_range("2024-01-01", periods=T, freq="D")
    codes = [f"S{i:04d}" for i in range(N)]

    df = pd.DataFrame(
        rng.standard_normal((T, N)), index=dates, columns=codes,
    )
    # 部分 NaN,触发各种边界
    df.iloc[5:10, :40] = np.nan          # 头部某些行大面积 NaN -> fallback
    df.iloc[:, ::17] = np.nan            # 列方向稀疏 NaN

    log_mcap = pd.DataFrame(
        rng.normal(15.0, 2.0, size=(T, N)), index=dates, columns=codes,
    )
    log_mcap.iloc[:, ::23] = np.nan      # 部分 code 在所有天都缺 mcap

    sector_map = {c: f"ind_{i % n_industries}" for i, c in enumerate(codes)}

    out_old = _industry_neutralize_per_day_loop(df, sector_map, log_mcap)
    out_new = industry_neutralize_panel(df, sector_map, log_mcap)

    # 容差:normal equation vs lstsq 在 condition number 较好时数值近似;
    # 退化日两条路径都走 group-demean fallback -> 应 bit-exact。
    np.testing.assert_allclose(
        out_new.values, out_old.values,
        rtol=1e-8, atol=1e-10, equal_nan=True,
    )


def test_industry_log_mcap_batch_no_log_mcap_unchanged():
    """log_mcap=None 路径不能被批量改动影响 — 严格 bit-exact。"""
    rng = np.random.default_rng(0)
    T, N = 20, 30
    df = pd.DataFrame(
        rng.standard_normal((T, N)),
        index=pd.date_range("2024-01-01", periods=T, freq="D"),
        columns=[f"S{i:04d}" for i in range(N)],
    )
    sector_map = {c: f"ind_{i % 3}" for i, c in enumerate(df.columns)}

    out_old = _industry_neutralize_per_day_loop(df, sector_map, log_mcap=None)
    out_new = industry_neutralize_panel(df, sector_map, log_mcap=None)
    pd.testing.assert_frame_equal(out_new, out_old, check_exact=True)
```

- [ ] **Step 2: 运行测试,确认两条新测试都通过(因为 Task 1.1 后 new == old)**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ml_preprocess_mcap.py::test_industry_log_mcap_batch_matches_legacy tests/test_ml_preprocess_mcap.py::test_industry_log_mcap_batch_no_log_mcap_unchanged -v`
Expected: PASS(等价 oracle 比对自己)

### Task 1.3: 实现批量 normal equation

**Files:**
- Modify: `src/stockpool/ml/preprocess.py:201-274`(替换 `industry_neutralize_panel` 函数体的 log_mcap 分支)

- [ ] **Step 1: 用批量 normal equation 替换 per-day loop**

把当前 `industry_neutralize_panel` 函数 log_mcap 分支(从 `# OLS path: build one-hot industry dummies` 开始到函数末尾)替换成:

```python
    # OLS path (vectorised, PR-T1.1):
    # 把 [industry_dummies | intercept | log_mcap] 拼成 (T, N, K+2) 三维 X,
    # 配合 valid_mask 用 einsum 算 X^T X 和 X^T y,np.linalg.solve 一次解 T 个系数。
    # 与 mcap_neutralize_panel 同范式。
    industries = pd.Series(
        {c: sector_map.get(c, "_unknown_") for c in df.columns},
    )
    dummies = pd.get_dummies(industries, prefix="ind", drop_first=True, dtype=float)
    # dummies.index = codes; dummies.columns = ind_<label> minus reference.

    log_mcap_aligned = log_mcap.reindex(index=df.index, columns=df.columns)

    # 预计算 group-demean fallback panel 一次,fallback 日按 date 复用。
    legacy_fallback = industry_neutralize_panel(df, sector_map, log_mcap=None)

    T = len(df.index)
    N = len(df.columns)
    K = dummies.shape[1]  # 行业 dummy 数(已 drop_first)
    F = K + 2             # +intercept +log_mcap

    Y = df.astype(float).values                     # (T, N)
    M = log_mcap_aligned.values                     # (T, N)
    valid = ~(np.isnan(Y) | np.isnan(M))            # (T, N)
    n_valid = valid.sum(axis=1)                     # (T,)

    # X shape: (T, N, F);dummies + intercept 在所有 t 上相同,log_mcap 沿 t 变化。
    # 用广播构造,避免 T 次显式 copy。
    dummies_TNK = np.broadcast_to(
        dummies.values[None, :, :], (T, N, K),
    )                                                # (T, N, K)
    intercept = np.ones((T, N, 1))                  # (T, N, 1)
    log_mcap_col = M[:, :, None]                    # (T, N, 1)
    X = np.concatenate([dummies_TNK, intercept, log_mcap_col], axis=2)  # (T, N, F)

    # invalid 行(NaN in Y 或 M)零化,等价于剔出回归。
    valid_3d = valid[:, :, None]                    # (T, N, 1)
    X_masked = np.where(valid_3d, X, 0.0)           # (T, N, F)
    Y_masked = np.where(valid, Y, 0.0)              # (T, N)

    # Normal equation: XtX (T, F, F), XtY (T, F)
    XtX = np.einsum("tnf,tng->tfg", X_masked, X_masked)
    XtY = np.einsum("tnf,tn->tf", X_masked, Y_masked)

    # 加 tiny ridge 防止行业完全空的 t rank 不足时 solve 崩。fallback 走 legacy。
    # 用 valid_mask 区分 — 不直接用 ridge 染指系数。
    coefs = np.full((T, F), np.nan, dtype=float)
    fallback_days = 0
    for t in range(T):
        if n_valid[t] < 10:
            fallback_days += 1
            continue
        try:
            coefs[t] = np.linalg.solve(XtX[t], XtY[t])
        except np.linalg.LinAlgError:
            fallback_days += 1

    # 残差 = Y - X · β 对每个 (t, n) 单独算
    # einsum 之前把 invalid_t 的 coefs 置 0 也行,但下面直接逐行算更清晰。
    fitted = np.einsum("tnf,tf->tn", X, coefs)       # (T, N) — fallback 行是 NaN
    resid = Y - fitted

    fallback_t_mask = np.isnan(coefs[:, 0])          # (T,) — fallback 行
    # fallback 日 -> 用 legacy_fallback 该天的整行(group demean)。
    out_vals = np.where(fallback_t_mask[:, None], legacy_fallback.values, resid)

    # 与原 per-row OLS 一致 — log_mcap 缺但 Y 在的 code 保留原 Y(不残化)。
    cell_preserve = np.isnan(M) & ~np.isnan(Y)
    out_vals = np.where(cell_preserve, Y, out_vals)

    if fallback_days:
        log.warning(
            "industry_neutralize_panel(log_mcap=...): OLS fallback on %d / %d days "
            "(degenerate cross-section); used group demean for those days",
            fallback_days, len(df.index),
        )
    return pd.DataFrame(out_vals, index=df.index, columns=df.columns)
```

- [ ] **Step 2: 运行 Task 1.2 写的测试,期望 PASS**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ml_preprocess_mcap.py::test_industry_log_mcap_batch_matches_legacy -v`
Expected: PASS(批量路径与 per-day oracle 在 atol=1e-10 / rtol=1e-8 内一致)

- [ ] **Step 3: 运行完整 preprocess 测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ml_preprocess.py tests/test_ml_preprocess_mcap.py -v`
Expected: 全部 PASS

- [ ] **Step 4: 跑全部测试套保险**

Run: `.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 615+ PASS,无新 FAIL

- [ ] **Step 5: Commit**

```bash
git add src/stockpool/ml/preprocess.py tests/test_ml_preprocess_mcap.py
git commit -m "perf(preprocess): vectorise industry+log_mcap joint OLS via batched normal equation"
```

### Task 1.4: 文档更新

**Files:**
- Modify: `CLAUDE.md` — 模块地图 `src/stockpool/ml/preprocess.py` 那一行

- [ ] **Step 1: 在 `CLAUDE.md` 对应模块地图行末追加一句**

找到 `src/stockpool/ml/preprocess.py` 的描述行(包含 "截面预处理流水线"),在末尾追加:

```
**PR-T1.1 性能**:`industry_neutralize_panel(log_mcap=...)` 的 per-day OLS loop 改造为批量 normal equation(与 mcap_neutralize_panel 同范式),T=1000 时 ~50× 加速。`_industry_neutralize_per_day_loop` 保留为 test oracle。
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude): note PR-T1.1 preprocess vectorisation"
```

---

## PR-T1.2 — Rust ops PR-4:`decay_linear` + `indneutralize`

预算:1.5-2 天 | 风险:中(部分窗口 + group encoding 边界)

**文件结构:**
- Create: `rust/stockpool_ops/src/decay.rs` — decay_linear Rust 实现
- Modify: `rust/stockpool_ops/src/cs.rs` — 追加 indneutralize 实现
- Modify: `rust/stockpool_ops/src/lib.rs` — 注册 2 个新 `#[pyfunction]`
- Modify: `src/stockpool/factors/ops.py` — 加 dispatcher
- Modify: `tests/test_ops_rust_equivalence.py` — 加 Layer A 单测

### Task 2.1: Rust decay.rs 框架(空函数 + 编译通过)

**Files:**
- Create: `rust/stockpool_ops/src/decay.rs`
- Modify: `rust/stockpool_ops/src/lib.rs`

- [ ] **Step 1: 新建 decay.rs 占位**

```rust
//! Linearly-weighted moving average (NaN-safe, partial-window aware).
//!
//! Mirrors `_ops_py.decay_linear`:
//!   * weights = 1..=d (oldest -> newest)
//!   * NaN positions excluded from numerator AND denominator,
//!     remaining weights renormalize
//!   * all-NaN window -> NaN
//!   * min_periods = max(1, int(d * 0.6))
//!   * partial windows (t+1 < d): weights[-len(window):] tail-aligned

use ndarray::{Array2, ArrayView2};

use crate::util::{min_periods, rolling_apply_col};

pub fn decay_linear(x: ArrayView2<f64>, d: usize) -> Array2<f64> {
    let mp = min_periods(d);
    // Pre-compute weights[1..=d] once; per-window we slice the tail.
    let full_weights: Vec<f64> = (1..=d).map(|i| i as f64).collect();
    rolling_apply_col(x, d, mp, false, move |window: &[f64]| {
        let w_slice = &full_weights[full_weights.len() - window.len()..];
        let mut num = 0.0;
        let mut den = 0.0;
        let mut any_valid = false;
        for (v, &w) in window.iter().zip(w_slice.iter()) {
            if v.is_finite() {
                num += v * w;
                den += w;
                any_valid = true;
            }
        }
        if !any_valid {
            f64::NAN
        } else {
            num / den
        }
    })
}
```

- [ ] **Step 2: 在 lib.rs 注册新 mod + pyfunction**

打开 `rust/stockpool_ops/src/lib.rs`,在 `mod cs;` 那行后面加 `mod decay;`:

```rust
mod cs;
mod decay;
mod rolling;
mod util;
```

在 `#[pyfunction] fn correlation_py` 之后(`#[pymodule]` 之前)追加:

```rust
/// Linearly-weighted moving average. Weights 1..=d (oldest -> newest);
/// NaN positions drop from numerator + denominator; min_periods = max(1, int(d*0.6)).
#[pyfunction]
#[pyo3(name = "decay_linear")]
fn decay_linear_py<'py>(
    py: Python<'py>,
    x: PyReadonlyArray2<'py, f64>,
    d: usize,
) -> Bound<'py, PyArray2<f64>> {
    let view = x.as_array();
    let out = py.allow_threads(|| decay::decay_linear(view, d));
    out.to_pyarray_bound(py)
}
```

在 `#[pymodule] fn stockpool_ops_rs` 函数体里 `m.add_function(wrap_pyfunction!(correlation_py, m)?)?;` 之后加:

```rust
    m.add_function(wrap_pyfunction!(decay_linear_py, m)?)?;
```

- [ ] **Step 3: 构建 Rust 产物(注意 unset CONDA_PREFIX)**

Run:
```bash
unset CONDA_PREFIX && .venv/Scripts/python.exe -m maturin develop --release --manifest-path rust/stockpool_ops/Cargo.toml
```
Expected: `Successfully built` 然后 `.so/.pyd` 安装到 `.venv`

如果失败,看 [maturin_conda_prefix_gotcha.md] memory 提示。

- [ ] **Step 4: 验证模块可导入**

Run:
```bash
.venv/Scripts/python.exe -c "import stockpool_ops_rs; print(dir(stockpool_ops_rs))"
```
Expected: 输出列表里包含 `decay_linear`

### Task 2.2: Layer A 单测 decay_linear(对照 pandas oracle)

**Files:**
- Modify: `tests/test_ops_rust_equivalence.py`

- [ ] **Step 1: 追加单测**

在文件末尾加:

```python
import pytest
import numpy as np
import pandas as pd

from stockpool.factors import _ops_py
import stockpool_ops_rs as _rust


def _df(arr, dates=None, codes=None):
    T, N = arr.shape
    dates = dates or pd.date_range("2024-01-01", periods=T, freq="D")
    codes = codes or [f"S{i:03d}" for i in range(N)]
    return pd.DataFrame(arr, index=dates, columns=codes)


@pytest.mark.parametrize("T,N,d", [
    (50, 20, 5),
    (30, 50, 10),
    (10, 5, 3),
])
def test_decay_linear_happy(T, N, d):
    rng = np.random.default_rng(42)
    arr = rng.standard_normal((T, N))
    df = _df(arr)
    rust_out = pd.DataFrame(
        _rust.decay_linear(np.ascontiguousarray(arr), d),
        index=df.index, columns=df.columns,
    )
    py_out = _ops_py.decay_linear(df, d)
    np.testing.assert_allclose(
        rust_out.values, py_out.values,
        rtol=1e-7, atol=1e-9, equal_nan=True,
    )


def test_decay_linear_all_nan_window():
    arr = np.full((10, 3), np.nan)
    arr[5:, :] = 1.0  # 后半 valid
    rust_out = _rust.decay_linear(arr, 5)
    py_out = _ops_py.decay_linear(_df(arr), 5).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)


def test_decay_linear_partial_window_mid():
    """d=20, 单列 12 个 valid 数据点(mid-partial window) — 权重尾对齐边界。"""
    arr = np.arange(1, 13, dtype=float).reshape(12, 1)
    rust_out = _rust.decay_linear(np.ascontiguousarray(arr), 20)
    py_out = _ops_py.decay_linear(_df(arr), 20).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)


def test_decay_linear_constant_input():
    arr = np.full((10, 3), 5.0)
    rust_out = _rust.decay_linear(arr, 3)
    py_out = _ops_py.decay_linear(_df(arr), 3).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)


def test_decay_linear_scattered_nan():
    rng = np.random.default_rng(7)
    arr = rng.standard_normal((30, 10))
    # 10% 散布 NaN
    nan_mask = rng.random((30, 10)) < 0.1
    arr[nan_mask] = np.nan
    rust_out = _rust.decay_linear(np.ascontiguousarray(arr), 5)
    py_out = _ops_py.decay_linear(_df(arr), 5).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)
```

- [ ] **Step 2: 运行单测**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ops_rust_equivalence.py -v -k decay_linear`
Expected: 全部 PASS

如果 FAIL,先重新 build(Task 2.1 Step 3),再排查 partial window 边界。

- [ ] **Step 3: Commit**

```bash
git add rust/stockpool_ops/src/decay.rs rust/stockpool_ops/src/lib.rs tests/test_ops_rust_equivalence.py
git commit -m "feat(rust-ops): port decay_linear to Rust + Layer A equivalence tests"
```

### Task 2.3: Rust indneutralize 实现 + 注册

**Files:**
- Modify: `rust/stockpool_ops/src/cs.rs` — 追加 indneutralize
- Modify: `rust/stockpool_ops/src/lib.rs` — 注册 pyfunction

- [ ] **Step 1: 在 cs.rs 末尾追加 indneutralize**

```rust
use numpy::ndarray::ArrayView1 as NpArrayView1;
use numpy::PyReadonlyArray1;

/// Group demean per row.
///
/// `sector_ids[c]` is the integer sector id for column c. Codes with id < 0
/// are treated as solo groups (output = x, since group mean of one valid
/// value equals the value itself, giving 0 after subtraction — matches the
/// pandas oracle's `__solo__<code>` semantics).
///
/// NaN cells are excluded from group means and remain NaN in the output.
pub fn indneutralize(
    x: ArrayView2<f64>,
    sector_ids: NpArrayView1<i32>,
) -> Array2<f64> {
    let (nrows, ncols) = x.dim();
    let mut out = Array2::from_elem((nrows, ncols), f64::NAN);
    // Compute max sector id once (skip negatives — solo bucket).
    let max_id: i32 = sector_ids
        .iter()
        .copied()
        .filter(|&v| v >= 0)
        .max()
        .unwrap_or(-1);
    if max_id < 0 {
        // All solo -> output is all zeros for finite cells, NaN otherwise.
        Zip::from(out.axis_iter_mut(Axis(0)))
            .and(x.axis_iter(Axis(0)))
            .par_for_each(|mut out_row, in_row| {
                for (o, v) in out_row.iter_mut().zip(in_row.iter()) {
                    if v.is_finite() {
                        *o = 0.0;
                    }
                }
            });
        return out;
    }
    let n_buckets = (max_id + 1) as usize;
    Zip::from(out.axis_iter_mut(Axis(0)))
        .and(x.axis_iter(Axis(0)))
        .par_for_each(|mut out_row, in_row| {
            let mut sum_by_sector = vec![0.0f64; n_buckets];
            let mut cnt_by_sector = vec![0usize; n_buckets];
            for (c, v) in in_row.iter().enumerate() {
                let sid = sector_ids[c];
                if sid < 0 || !v.is_finite() {
                    continue;
                }
                sum_by_sector[sid as usize] += *v;
                cnt_by_sector[sid as usize] += 1;
            }
            for (c, v) in in_row.iter().enumerate() {
                if !v.is_finite() {
                    continue;
                }
                let sid = sector_ids[c];
                if sid < 0 {
                    out_row[c] = 0.0;  // solo: self - self
                    continue;
                }
                let n = cnt_by_sector[sid as usize];
                if n == 0 {
                    continue;  // shouldn't happen since v is finite
                }
                let mean = sum_by_sector[sid as usize] / (n as f64);
                out_row[c] = *v - mean;
            }
        });
    out
}
```

- [ ] **Step 2: 在 lib.rs 注册 `#[pyfunction]`**

在 `#[pyfunction] fn decay_linear_py` 之后追加:

```rust
/// Group demean per row. sector_ids[c] = integer sector id for column c
/// (negative = solo group / not in map).
#[pyfunction]
#[pyo3(name = "indneutralize")]
fn indneutralize_py<'py>(
    py: Python<'py>,
    x: PyReadonlyArray2<'py, f64>,
    sector_ids: numpy::PyReadonlyArray1<'py, i32>,
) -> Bound<'py, PyArray2<f64>> {
    let x_view = x.as_array();
    let s_view = sector_ids.as_array();
    let out = py.allow_threads(|| cs::indneutralize(x_view, s_view));
    out.to_pyarray_bound(py)
}
```

在 `#[pymodule]` 内 `m.add_function(wrap_pyfunction!(decay_linear_py, m)?)?;` 之后追加:

```rust
    m.add_function(wrap_pyfunction!(indneutralize_py, m)?)?;
```

- [ ] **Step 3: 重新 build**

Run:
```bash
unset CONDA_PREFIX && .venv/Scripts/python.exe -m maturin develop --release --manifest-path rust/stockpool_ops/Cargo.toml
```
Expected: `Successfully built`

- [ ] **Step 4: 验证导入**

Run: `.venv/Scripts/python.exe -c "import stockpool_ops_rs; assert hasattr(stockpool_ops_rs, 'indneutralize')"`
Expected: 无 AssertionError

### Task 2.4: Layer A 单测 indneutralize

**Files:**
- Modify: `tests/test_ops_rust_equivalence.py`

- [ ] **Step 1: 在文件末尾追加 helper + 测试**

```python
def _encode_sectors(codes, sector_map):
    """{code -> str sector} -> int32 array;缺 map 的 code 编 -1。"""
    label_to_id = {}
    sector_ids = np.empty(len(codes), dtype=np.int32)
    for i, c in enumerate(codes):
        s = sector_map.get(c)
        if s is None:
            sector_ids[i] = -1
            continue
        if s not in label_to_id:
            label_to_id[s] = len(label_to_id)
        sector_ids[i] = label_to_id[s]
    return sector_ids


@pytest.mark.parametrize("T,N,n_sec", [
    (20, 30, 3),
    (50, 100, 8),
    (5, 12, 2),
])
def test_indneutralize_happy(T, N, n_sec):
    rng = np.random.default_rng(1)
    arr = rng.standard_normal((T, N))
    codes = [f"S{i:04d}" for i in range(N)]
    sector_map = {c: f"ind_{i % n_sec}" for i, c in enumerate(codes)}
    sector_ids = _encode_sectors(codes, sector_map)
    rust_out = _rust.indneutralize(np.ascontiguousarray(arr), sector_ids)
    py_out = _ops_py.indneutralize(_df(arr, codes=codes), sector_map).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)


def test_indneutralize_with_nan():
    rng = np.random.default_rng(2)
    arr = rng.standard_normal((10, 8))
    arr[0:3, 0] = np.nan
    arr[5, :] = np.nan
    codes = [f"S{i}" for i in range(8)]
    sector_map = {c: ("A" if i < 4 else "B") for i, c in enumerate(codes)}
    sector_ids = _encode_sectors(codes, sector_map)
    rust_out = _rust.indneutralize(np.ascontiguousarray(arr), sector_ids)
    py_out = _ops_py.indneutralize(_df(arr, codes=codes), sector_map).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)


def test_indneutralize_missing_code_solo_group():
    """Codes 不在 sector_map -> Rust 端 sector_id=-1 -> 输出 0(self-self)。"""
    rng = np.random.default_rng(3)
    arr = rng.standard_normal((5, 4))
    codes = ["A", "B", "C", "D"]
    sector_map = {"A": "x", "B": "x"}  # C, D 不在 map
    sector_ids = _encode_sectors(codes, sector_map)
    rust_out = _rust.indneutralize(np.ascontiguousarray(arr), sector_ids)
    py_out = _ops_py.indneutralize(_df(arr, codes=codes), sector_map).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)


def test_indneutralize_single_member_group():
    """单成员行业 -> mean = self -> demeaned = 0。"""
    arr = np.array([[1.0, 2.0, 3.0]])
    codes = ["A", "B", "C"]
    sector_map = {"A": "x", "B": "y", "C": "z"}  # 每个 code 自成一组
    sector_ids = _encode_sectors(codes, sector_map)
    rust_out = _rust.indneutralize(np.ascontiguousarray(arr), sector_ids)
    py_out = _ops_py.indneutralize(_df(arr, codes=codes), sector_map).values
    np.testing.assert_allclose(rust_out, py_out, rtol=1e-7, atol=1e-9, equal_nan=True)
```

- [ ] **Step 2: 运行测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ops_rust_equivalence.py -v -k indneutralize`
Expected: 全部 PASS

- [ ] **Step 3: Commit**

```bash
git add rust/stockpool_ops/src/cs.rs rust/stockpool_ops/src/lib.rs tests/test_ops_rust_equivalence.py
git commit -m "feat(rust-ops): port indneutralize to Rust + Layer A equivalence tests"
```

### Task 2.5: 接入 ops.py dispatcher

**Files:**
- Modify: `src/stockpool/factors/ops.py:26-30`(imports)和 `46-103` 区域(加 dispatcher)

- [ ] **Step 1: 改 ops.py 顶部 import 与 dispatcher**

把 `src/stockpool/factors/ops.py` 26-30 行改成只 import oracle:

```python
from . import _ops_py as _py_ops
```

(删除 `from ._ops_py import correlation, decay_linear, indneutralize` 这 3 行 — Task 2.5 之后每个都走 dispatcher)

在 `__all__` 之前(`def ts_rank` 之后)追加两个 dispatcher:

```python
def decay_linear(x, d):
    """Linearly-weighted moving average; weights 1..=d normalised, NaN-safe."""
    if _USE_RUST:
        import numpy as _np
        import pandas as _pd
        arr = _np.ascontiguousarray(x.to_numpy(), dtype=_np.float64)
        out_arr = _rust.decay_linear(arr, int(d))
        return _pd.DataFrame(out_arr, index=x.index, columns=x.columns)
    return _py_ops.decay_linear(x, d)


def indneutralize(x, group):
    """Cross-sectional demean within sector groups (NaN-safe).

    ``group`` 是 ``{code -> sector_name}`` dict 或 Series — dispatcher 内部
    会编成 ``int32`` sector_id 数组喂给 Rust 端;缺 map 的 code 编 -1。
    """
    if _USE_RUST:
        import numpy as _np
        import pandas as _pd
        # 编码:连续 0..K-1;缺 map -> -1。
        if hasattr(group, "to_dict"):
            gmap = group.to_dict()
        else:
            gmap = dict(group)
        label_to_id: dict[str, int] = {}
        sector_ids = _np.empty(len(x.columns), dtype=_np.int32)
        for i, c in enumerate(x.columns):
            s = gmap.get(c)
            if s is None:
                sector_ids[i] = -1
                continue
            if s not in label_to_id:
                label_to_id[s] = len(label_to_id)
            sector_ids[i] = label_to_id[s]
        arr = _np.ascontiguousarray(x.to_numpy(), dtype=_np.float64)
        out_arr = _rust.indneutralize(arr, sector_ids)
        return _pd.DataFrame(out_arr, index=x.index, columns=x.columns)
    return _py_ops.indneutralize(x, group)


def correlation(x, y, d):
    """Per-column trailing-d Pearson correlation (cleaned for FP noise)."""
    if _USE_RUST:
        import numpy as _np
        import pandas as _pd
        x_arr = _np.ascontiguousarray(x.to_numpy(), dtype=_np.float64)
        y_arr = _np.ascontiguousarray(y.to_numpy(), dtype=_np.float64)
        out_arr = _rust.correlation(x_arr, y_arr, int(d))
        return _pd.DataFrame(out_arr, index=x.index, columns=x.columns)
    return _py_ops.correlation(x, y, d)
```

(注意:`correlation` dispatcher 之前其实没在 ops.py 写出来 — Task 2.5 顺手补上,与已有 `rank` / `ts_std` 等的范式一致。)

- [ ] **Step 2: 运行算子对照(snapshot)测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ops_snapshot.py -v`
Expected: PASS — 已有 fixture 严格比对全 167 个因子,Rust 路径全开后仍一致

- [ ] **Step 3: 运行 Rust 等价性测试全集 + factor 测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ops_rust_equivalence.py tests/test_factors.py tests/test_wq101.py -q`
Expected: 全部 PASS

- [ ] **Step 4: 跑全套**

Run: `.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 615+ PASS

- [ ] **Step 5: 强制走 oracle 回归(确保 USE_PYTHON_OPS=1 不破坏)**

Run: `STOCKPOOL_USE_PYTHON_OPS=1 .venv/Scripts/python.exe -m pytest tests/test_ops_snapshot.py tests/test_wq101.py -q`
Expected: PASS — 回滚开关一切正常

- [ ] **Step 6: Commit**

```bash
git add src/stockpool/factors/ops.py
git commit -m "feat(factors): wire decay_linear + indneutralize + correlation to Rust dispatcher"
```

### Task 2.6: 文档更新

**Files:**
- Modify: `CLAUDE.md` — ops.py 模块地图行

- [ ] **Step 1: 把 ops.py 行的 "7 个 hot op" 改为 "9 个 hot op"**

找到 `src/stockpool/factors/ops.py` 那行,把:
```
| `src/stockpool/factors/ops.py` | **WQ 算子库** ... 7 个 hot op 自动 dispatch 到 Rust 加速 ...
```
改为:
```
| `src/stockpool/factors/ops.py` | **WQ 算子库** ... 9 个 hot op 自动 dispatch 到 Rust 加速 (含 PR-T1.2 加入的 `decay_linear` + `indneutralize`) ...
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude): note 9 hot ops dispatched to Rust (decay_linear + indneutralize)"
```

---

## PR-T1.3 — `StaggeredRunner` 并行化 N 个 offset

预算:0.5-1 天 | 风险:中(pickle 闭包 → 用 components 传参绕开)

**文件结构:**
- Modify: `src/stockpool/portfolio/ensemble.py` — 加 `components` 字段 + `parallel` 参数 + 顶层 worker
- Modify: `src/stockpool/config.py:570-592` — 加 `parallel_staggered: bool = False`
- Modify: `src/stockpool/cli.py:cmd_portfolio_backtest`(434 起)— 传入 parallel flag
- Modify: `src/stockpool/portfolio_ab/runner.py:173-194` — 改成传 components
- Modify: `tests/test_portfolio_ensemble.py` — 新增并行等价性测试
- Create: `scripts/verify_staggered_parallel.py` — minimal example 校验脚本

### Task 3.1: 加配置字段

**Files:**
- Modify: `src/stockpool/config.py:570-592`

- [ ] **Step 1: 在 PortfolioBacktestConfig 加 `parallel_staggered: bool = False`**

打开 `config.py`,在 `staggered_starts: int = Field(default=1, ge=1, le=20)` 这行**之后**插入:

```python
    parallel_staggered: bool = False
    """PR-T1.3: 并行跑 N 个 staggered offset(opt-in)。需要 engine 的
    components (strategy, portfolio_cfg, costs, risk_free_rate, eligibility,
    sector_map) 全部可 pickle — 通常都满足。失败 fallback 串行 + warning。"""
```

- [ ] **Step 2: 运行 config 测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config.py -q`
Expected: PASS

### Task 3.2: 写失败测试

**Files:**
- Modify: `tests/test_portfolio_ensemble.py`

- [ ] **Step 1: 在文件末尾追加并行等价性测试**

```python
def test_parallel_matches_serial(make_panel_data, make_strategy_factory):
    """parallel=True vs serial 跑 staggered 应 bit-exact(engine 确定性)。"""
    panel_data = make_panel_data(n_codes=8, n_bars=40)
    engine_factory, components = make_strategy_factory()

    runner_serial = StaggeredRunner(engine_factory, risk_free_rate=0.02)
    ens_serial = runner_serial.run(panel_data, n_offsets=3)

    runner_par = StaggeredRunner(
        engine_factory, components=components, risk_free_rate=0.02,
    )
    ens_par = runner_par.run(panel_data, n_offsets=3, parallel=True)

    np.testing.assert_array_equal(
        ens_serial.ensemble_curve["equity"].values,
        ens_par.ensemble_curve["equity"].values,
    )
    np.testing.assert_array_equal(
        ens_serial.envelope[["min", "p25", "median", "p75", "max"]].values,
        ens_par.envelope[["min", "p25", "median", "p75", "max"]].values,
    )
    assert ens_serial.aggregated_metrics["ensemble"] == ens_par.aggregated_metrics["ensemble"]
```

文件头部如果还没有 `import numpy as np` 和 `from stockpool.portfolio.ensemble import StaggeredRunner`,补上。

**fixture 复用**:文件已经有 `make_panel_data` / `make_strategy_factory` 一类 fixture(在文件已有的其他 test 中找)。如果旧 fixture 不返回 `components`,在 fixture 里扩展:返回 `(factory, components)` 二元组,components = `(strategy, portfolio_cfg, costs, risk_free_rate, eligibility, sector_map)` 与现有 `_factory` 内 PortfolioEngine 构造参数一致。

- [ ] **Step 2: 跑测试,期望 FAIL**

Run: `.venv/Scripts/python.exe -m pytest tests/test_portfolio_ensemble.py::test_parallel_matches_serial -v`
Expected: FAIL(StaggeredRunner 还没有 components 参数 / parallel 参数)

### Task 3.3: 改 StaggeredRunner 支持 components + parallel

**Files:**
- Modify: `src/stockpool/portfolio/ensemble.py:48-74`(StaggeredRunner 类)

- [ ] **Step 1: 顶层 worker 函数 + 改 `__init__` + 改 `run`**

把 `class StaggeredRunner` 前面加一个 module-top-level helper:

```python
def _run_one_offset_in_subprocess(components, panel_data, k):
    """Worker (must be top-level for pickle).

    components = (strategy, portfolio_cfg, costs, risk_free_rate,
                  eligibility, sector_map)
    """
    strategy, portfolio_cfg, costs, risk_free_rate, eligibility, sector_map = components
    engine = PortfolioEngine(
        strategy=strategy,
        portfolio_cfg=portfolio_cfg,
        costs=costs,
        risk_free_rate=risk_free_rate,
        eligibility=eligibility,
        sector_map=sector_map,
    )
    return engine.run(panel_data, start_offset=k)
```

然后改 `StaggeredRunner.__init__`:

```python
    def __init__(
        self,
        engine_factory: Callable[[], PortfolioEngine] | None = None,
        risk_free_rate: float = 0.02,
        components: tuple | None = None,
    ):
        """PR-T1.3: components(可 pickle 元组)是 parallel mode 必需的备用入口。
        engine_factory 是串行 mode 的传统入口;两者至少需要一个。
        """
        if engine_factory is None and components is None:
            raise ValueError(
                "StaggeredRunner needs either engine_factory or components"
            )
        self._engine_factory = engine_factory
        self._components = components
        self.risk_free_rate = risk_free_rate
```

改 `run`:

```python
    def run(
        self,
        panel_data: Mapping[str, pd.DataFrame],
        n_offsets: int,
        parallel: bool = False,
    ) -> EnsembleResult:
        if n_offsets < 1:
            raise ValueError(f"n_offsets must be >= 1, got {n_offsets}")
        if parallel and self._components is not None and n_offsets > 1:
            try:
                import concurrent.futures
                import os
                max_workers = min(n_offsets, os.cpu_count() or 1)
                with concurrent.futures.ProcessPoolExecutor(
                    max_workers=max_workers,
                ) as ex:
                    futures = [
                        ex.submit(
                            _run_one_offset_in_subprocess,
                            self._components, panel_data, k,
                        )
                        for k in range(n_offsets)
                    ]
                    results = [f.result() for f in futures]
                return self._aggregate(results)
            except Exception as e:  # noqa: BLE001
                import logging
                logging.getLogger("stockpool").warning(
                    "parallel staggered failed (%s); fallback to serial", e,
                )
        # Serial path (default; also fallback)
        results: list[PortfolioBacktestResult] = []
        for k in range(n_offsets):
            if self._engine_factory is not None:
                engine = self._engine_factory()
            else:
                # components-only mode: rebuild engine in main process.
                from stockpool.portfolio.engine import PortfolioEngine
                strategy, portfolio_cfg, costs, rfr, eligibility, sector_map = self._components
                engine = PortfolioEngine(
                    strategy=strategy, portfolio_cfg=portfolio_cfg, costs=costs,
                    risk_free_rate=rfr, eligibility=eligibility, sector_map=sector_map,
                )
            results.append(engine.run(panel_data, start_offset=k))
        return self._aggregate(results)
```

- [ ] **Step 2: 跑测试,期望 PASS**

Run: `.venv/Scripts/python.exe -m pytest tests/test_portfolio_ensemble.py -v`
Expected: 全部 PASS(包括新的 `test_parallel_matches_serial`)

如果 fixture 不支持 components,先回 Task 3.2 修 fixture。

- [ ] **Step 3: Commit**

```bash
git add src/stockpool/portfolio/ensemble.py src/stockpool/config.py tests/test_portfolio_ensemble.py
git commit -m "perf(portfolio): parallel staggered ensemble (opt-in via parallel_staggered cfg)"
```

### Task 3.4: 接入 cmd_portfolio_backtest

**Files:**
- Modify: `src/stockpool/cli.py:cmd_portfolio_backtest`(434 起)
- Modify: `src/stockpool/portfolio_ab/runner.py:173-194`

- [ ] **Step 1: 改 cli.py 的 cmd_portfolio_backtest 构造 StaggeredRunner**

定位到 `cmd_portfolio_backtest` 内构造 `StaggeredRunner(_factory, risk_free_rate=...)` 的那段。改成同时传 components 并按 cfg.portfolio_backtest.parallel_staggered 透传 parallel flag。具体改动:

```python
# 旧:
runner = StaggeredRunner(_factory, risk_free_rate=cfg.backtest.risk_free_rate)
ensemble = runner.run(portfolio_pool_data, n_offsets=n_offsets)

# 新:
components = (
    portfolio_strat, cfg.portfolio_backtest.portfolio, costs,
    cfg.backtest.risk_free_rate, eligibility, sector_map,
)
runner = StaggeredRunner(
    _factory, components=components, risk_free_rate=cfg.backtest.risk_free_rate,
)
ensemble = runner.run(
    portfolio_pool_data, n_offsets=n_offsets,
    parallel=cfg.portfolio_backtest.parallel_staggered,
)
```

- [ ] **Step 2: 改 portfolio_ab/runner.py 同样模式**

定位到 `portfolio_ab/runner.py:185`(`runner = StaggeredRunner(_factory, ...)`),把那段改成:

```python
components = (
    portfolio_strat, effective_cfg.portfolio_backtest.portfolio, costs,
    effective_cfg.backtest.risk_free_rate, eligibility, sector_map,
)
runner = StaggeredRunner(
    _factory, components=components,
    risk_free_rate=effective_cfg.backtest.risk_free_rate,
)
ensemble = runner.run(
    portfolio_pool_data, n_offsets=n_offsets,
    parallel=effective_cfg.portfolio_backtest.parallel_staggered,
)
```

- [ ] **Step 3: 跑 CLI 测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_cli_portfolio_backtest.py tests/test_cli_portfolio_ab.py tests/test_portfolio_ab_runner.py -v`
Expected: 全部 PASS(默认 parallel_staggered=False 路径完全不变)

### Task 3.5: 写 minimal example 校验脚本

**Files:**
- Create: `scripts/verify_staggered_parallel.py`

- [ ] **Step 1: 新建脚本**

```python
"""Minimal example 校验:Staggered serial vs parallel 在 bit-exact 容差内一致。

Usage:
    .venv/Scripts/python.exe scripts/verify_staggered_parallel.py

跑完看到 'OK: serial == parallel (bit-exact)' 即通过。
"""
from __future__ import annotations

import numpy as np
import pandas as pd

from stockpool.backtesting.framework import TradeCosts
from stockpool.config import PortfolioRunConfig
from stockpool.portfolio.engine import PortfolioEngine
from stockpool.portfolio.ensemble import StaggeredRunner
from stockpool.portfolio.strategy import PrecomputedScoreStrategy


def _make_panel(n_codes=8, n_bars=60, seed=42):
    rng = np.random.default_rng(seed)
    dates = pd.date_range("2024-01-02", periods=n_bars, freq="B")
    panel = {}
    for i in range(n_codes):
        code = f"S{i:03d}"
        prices = 10.0 * np.cumprod(1 + rng.normal(0.0005, 0.02, n_bars))
        panel[code] = pd.DataFrame({
            "date": dates,
            "open": prices * (1 + rng.normal(0, 0.001, n_bars)),
            "high": prices * 1.02,
            "low": prices * 0.98,
            "close": prices,
            "volume": rng.integers(1e5, 5e6, n_bars),
        })
    return panel, dates


def main():
    panel, dates = _make_panel()
    codes = list(panel.keys())
    rng = np.random.default_rng(0)
    scores = pd.DataFrame(
        rng.standard_normal((len(dates), len(codes))),
        index=dates, columns=codes,
    )
    strategy = PrecomputedScoreStrategy(scores, name="test")
    portfolio_cfg = PortfolioRunConfig(top_k=3, rebalance_n_days=5)
    costs = TradeCosts(buy_cost=0.001, sell_cost=0.001)
    components = (strategy, portfolio_cfg, costs, 0.02, None, {})

    def _factory():
        return PortfolioEngine(
            strategy=strategy, portfolio_cfg=portfolio_cfg, costs=costs,
            risk_free_rate=0.02, eligibility=None, sector_map={},
        )

    runner = StaggeredRunner(_factory, components=components, risk_free_rate=0.02)
    ens_serial = runner.run(panel, n_offsets=3, parallel=False)
    ens_par = runner.run(panel, n_offsets=3, parallel=True)

    np.testing.assert_array_equal(
        ens_serial.ensemble_curve["equity"].values,
        ens_par.ensemble_curve["equity"].values,
    )
    print(f"OK: serial == parallel (bit-exact); n_bars={len(dates)}, n_codes={len(codes)}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 跑脚本**

Run: `.venv/Scripts/python.exe scripts/verify_staggered_parallel.py`
Expected: stdout 输出 `OK: serial == parallel (bit-exact); ...`

- [ ] **Step 3: Commit**

```bash
git add src/stockpool/cli.py src/stockpool/portfolio_ab/runner.py scripts/verify_staggered_parallel.py
git commit -m "feat(portfolio): wire parallel_staggered through cli + portfolio_ab + verify script"
```

### Task 3.6: 文档更新

**Files:**
- Modify: `CLAUDE.md` — portfolio framework 段
- Modify: `README.md` — 如有用户可见的 yaml 字段说明

- [ ] **Step 1: 更新 CLAUDE.md 的 portfolio 段**

在 `portfolio_backtest` 配置说明那里追加一句:

```
**`parallel_staggered: bool = False`**(PR-T1.3):opt-in,N 个 staggered offset 并行跑(`ProcessPoolExecutor`)。要求 engine components 全部可 pickle。
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude): note parallel_staggered cfg field"
```

---

## PR-T1.4 — `portfolio_ab` 两 arm 并行

预算:0.5 天 | 风险:中(subprocess pickle 大量 state)

**文件结构:**
- Modify: `src/stockpool/portfolio_ab/runner.py` — 加 `parallel_arms` 参数 + 顶层 worker
- Modify: `src/stockpool/cli.py:cmd_portfolio_ab` 和 `cmd_ab` — 加 `--parallel-arms` flag
- Modify: `tests/test_portfolio_ab_runner.py` — 加并行等价测试
- Create: `scripts/verify_parallel_arms.py` — 端到端 minimal example

### Task 4.1: 顶层 worker + parallel 路径

**Files:**
- Modify: `src/stockpool/portfolio_ab/runner.py`

- [ ] **Step 1: 在 `run_single_arm` 函数之前加顶层 worker**

```python
def _run_single_arm_in_subprocess(
    arm_name, ab_cfg, base_cfg, override,
    pool_data, sector_map, name_map,
    refresh_scores, portfolio_pool_data, n_workers,
):
    """Top-level worker for ProcessPoolExecutor (must be picklable)."""
    from stockpool.portfolio_ab.config import build_effective_cfg
    try:
        effective = build_effective_cfg(
            base_cfg, override, use_ab_pool=ab_cfg.use_ab_pool,
        )
    except Exception as e:  # noqa: BLE001
        return ArmResult(
            name=arm_name, effective_cfg=None,
            failed=True, error=f"merge failed: {e}",
        )
    return run_single_arm(
        arm_name, effective,
        pool_data=pool_data, sector_map=sector_map, name_map=name_map,
        refresh_scores=refresh_scores,
        portfolio_pool_data=portfolio_pool_data,
        n_workers=n_workers,
    )
```

- [ ] **Step 2: 改 `run_portfolio_ab` 加 parallel_arms 参数**

把现有 `run_portfolio_ab` 改成:

```python
def run_portfolio_ab(
    ab_cfg: PortfolioABConfig,
    base_cfg: AppConfig,
    pool_data: Mapping[str, pd.DataFrame],
    sector_map: Mapping[str, str],
    name_map: Mapping[str, str],
    refresh_scores: bool = False,
    portfolio_pool_data: Mapping[str, pd.DataFrame] | None = None,
    n_workers: int | None = None,
    parallel_arms: bool = False,
) -> ABResult:
    """Run both arms; sequential by default. ``parallel_arms=True``
    runs arms concurrently in subprocess (peak memory ~2× single arm).
    """
    if parallel_arms and len(ab_cfg.arms) > 1:
        import concurrent.futures
        log.warning(
            "[parallel-arms] both arms running concurrently; "
            "peak memory ~= 2× single-arm. Disable with parallel_arms=False.",
        )
        try:
            with concurrent.futures.ProcessPoolExecutor(
                max_workers=len(ab_cfg.arms),
            ) as ex:
                futures = {
                    arm_name: ex.submit(
                        _run_single_arm_in_subprocess,
                        arm_name, ab_cfg, base_cfg, override,
                        pool_data, sector_map, name_map,
                        refresh_scores, portfolio_pool_data, n_workers,
                    )
                    for arm_name, override in ab_cfg.arms.items()
                }
                arms = {n: f.result() for n, f in futures.items()}
            return ABResult(arms=arms)
        except Exception as e:  # noqa: BLE001
            log.warning(
                "parallel_arms failed (%s); fallback to serial", e,
            )

    # Serial path (default; also fallback)
    arms: dict[str, ArmResult] = {}
    for arm_name, override in ab_cfg.arms.items():
        try:
            effective = build_effective_cfg(base_cfg, override, use_ab_pool=ab_cfg.use_ab_pool)
        except Exception as e:  # noqa: BLE001
            log.error("[%s] effective-cfg merge failed: %s", arm_name, e)
            arms[arm_name] = ArmResult(
                name=arm_name, effective_cfg=None,
                failed=True, error=f"merge failed: {e}",
            )
            continue
        arms[arm_name] = run_single_arm(
            arm_name, effective,
            pool_data=pool_data, sector_map=sector_map, name_map=name_map,
            refresh_scores=refresh_scores,
            portfolio_pool_data=portfolio_pool_data,
            n_workers=n_workers,
        )
    return ABResult(arms=arms)
```

### Task 4.2: 等价性测试

**Files:**
- Modify: `tests/test_portfolio_ab_runner.py`

- [ ] **Step 1: 在文件末尾追加测试**

```python
def test_parallel_arms_matches_serial(base_cfg, monkeypatch, tmp_path):
    """parallel_arms=True 与 serial 的 ArmResult.primary_metrics 应完全一致。

    前提:第一次跑(serial)写入 score_panel parquet 后,第二次跑(parallel)
    从 parquet 读 → 两条路径输入完全一致 → 输出 bit-exact。
    """
    # 准备最小 ab_cfg(2 arm)+ pool_data + sector/name maps
    # ... 复用 test_run_portfolio_ab_happy 的 fixture setup ...
    ab_cfg, pool_data, sector_map, name_map = _build_minimal_ab(base_cfg, tmp_path)

    res_serial = run_portfolio_ab(
        ab_cfg, base_cfg, pool_data, sector_map, name_map,
        parallel_arms=False,
    )
    res_parallel = run_portfolio_ab(
        ab_cfg, base_cfg, pool_data, sector_map, name_map,
        parallel_arms=True, refresh_scores=False,
    )

    for arm_name in ab_cfg.arms:
        m_s = res_serial.arms[arm_name].primary_metrics
        m_p = res_parallel.arms[arm_name].primary_metrics
        assert set(m_s) == set(m_p), f"{arm_name} keys differ"
        for k in m_s:
            v_s, v_p = m_s[k], m_p[k]
            if isinstance(v_s, float) and np.isnan(v_s):
                assert np.isnan(v_p), f"{arm_name}.{k}: serial NaN, parallel {v_p}"
            else:
                assert v_s == v_p, f"{arm_name}.{k}: {v_s} vs {v_p}"
        pd.testing.assert_series_equal(
            res_serial.arms[arm_name].primary_curve["equity"].reset_index(drop=True),
            res_parallel.arms[arm_name].primary_curve["equity"].reset_index(drop=True),
            check_exact=True,
        )
```

`_build_minimal_ab` helper 如果文件里没有,加一个:

```python
def _build_minimal_ab(base_cfg, tmp_path):
    """8 stocks × 30 bars 的合成 panel + 2 arm ab cfg。"""
    # 实现细节:看 test_run_portfolio_ab_happy 怎么 setup 就抄,
    # 关键是 score_cache_dir 指到 tmp_path,parallel 跑时复用 serial 写的 parquet。
    ...
```

(具体 setup 参照 `test_run_portfolio_ab_happy` 已有写法 — workers 应该会读懂并照搬。)

- [ ] **Step 2: 跑测试 — 先 FAIL(parallel_arms 还没接入 run 内部)**

Run: `.venv/Scripts/python.exe -m pytest tests/test_portfolio_ab_runner.py::test_parallel_arms_matches_serial -v`
Expected: 应该 PASS,因为 Task 4.1 已经接入了。如果意外 FAIL,看是不是 score_cache_dir 没复用导致 ML refit seed 漂移。

- [ ] **Step 3: 跑全部 portfolio_ab 测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_portfolio_ab_runner.py tests/test_portfolio_ab_config.py tests/test_cli_portfolio_ab.py -v`
Expected: 全部 PASS

### Task 4.3: CLI flag

**Files:**
- Modify: `src/stockpool/cli.py` — `cmd_portfolio_ab` 和 `cmd_ab` 函数 + 对应 argparse 注册

- [ ] **Step 1: 加 argparse flag(在 1186 起 p_pab.add_argument 块)**

定位到 `cli.py` 1189-1199 一段(`p_pab.add_argument` 配置 portfolio-ab 子命令)。在最后一行 `p_pab.set_defaults(...)` 之前插入:

```python
    p_pab.add_argument(
        "--parallel-arms", action="store_true",
        help="Run the two arms concurrently in subprocess (peak memory ~2× single arm).",
    )
```

同样找到 `cmd_ab` 的 argparse 注册(1145 附近 `p_ab.add_argument(--no-share-pool, ...)`),在最后加同样的 flag。

- [ ] **Step 2: 在 cmd_portfolio_ab 函数体内读 flag 并透传**

定位到 `cli.py:cmd_portfolio_ab` 调用 `run_portfolio_ab(...)` 的那行(778 附近),把它改成:

```python
result = run_portfolio_ab(
    ab_cfg, base_cfg, pool_data, sector_map, name_map,
    refresh_scores=args.refresh_scores if hasattr(args, "refresh_scores") else False,
    portfolio_pool_data=portfolio_pool_data,
    n_workers=getattr(args, "workers", None),
    parallel_arms=args.parallel_arms,
)
```

(对应 `cmd_ab` 也在调用 ab `run_ab` 时 — 如果 `run_ab` 还没 parallel_arms 参数,这步省略 / 改在 follow-up PR 处理)

- [ ] **Step 3: 跑 CLI 测试**

Run: `.venv/Scripts/python.exe -m pytest tests/test_cli_portfolio_ab.py -v`
Expected: PASS

### Task 4.4: minimal example 校验脚本

**Files:**
- Create: `scripts/verify_parallel_arms.py`

- [ ] **Step 1: 新建脚本**

```python
"""Minimal example 校验:portfolio-ab 两 arm 串行 vs 并行结果一致。

Usage:
    .venv/Scripts/python.exe scripts/verify_parallel_arms.py --config tests/fixtures/portfolio_ab_small.yaml

Prereq:
    需要事先有一份小型 portfolio_ab.yaml(2 arm, ~30 stocks, 1 年历史) +
    对应 base config + cache_dir 已经 warm 起来。

跑完看到 'OK: serial == parallel for all arms (bit-exact)' 即通过。
"""
from __future__ import annotations

import argparse
import numpy as np
import pandas as pd

from stockpool.config import load_config
from stockpool.fetcher import load_universe_cache
from stockpool.portfolio_ab.config import load_portfolio_ab_config
from stockpool.portfolio_ab.runner import run_portfolio_ab


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--config", required=True,
                        help="path to portfolio_ab.yaml")
    args = parser.parse_args()

    ab_cfg = load_portfolio_ab_config(args.config)
    base_cfg = load_config(ab_cfg.base_config)

    # 装 pool_data + sector_map + name_map(参照 cmd_portfolio_ab 的 setup)
    pool_data = load_universe_cache(base_cfg.data.cache_dir)
    # 简化:从 universe.parquet 派生 sector_map / name_map
    universe = pd.read_parquet(f"{base_cfg.data.cache_dir}/universe.parquet")
    sector_map = dict(zip(universe["code"], universe.get("industry", [""] * len(universe))))
    name_map = dict(zip(universe["code"], universe["name"]))

    res_serial = run_portfolio_ab(
        ab_cfg, base_cfg, pool_data, sector_map, name_map,
        parallel_arms=False,
    )
    res_par = run_portfolio_ab(
        ab_cfg, base_cfg, pool_data, sector_map, name_map,
        parallel_arms=True, refresh_scores=False,
    )

    for arm in ab_cfg.arms:
        m_s = res_serial.arms[arm].primary_metrics
        m_p = res_par.arms[arm].primary_metrics
        for k in m_s:
            v_s, v_p = m_s[k], m_p[k]
            if isinstance(v_s, float) and np.isnan(v_s):
                assert np.isnan(v_p), f"{arm}.{k}: NaN mismatch"
            else:
                assert v_s == v_p, f"{arm}.{k}: serial={v_s} parallel={v_p}"
        # equity curve bit-exact
        pd.testing.assert_series_equal(
            res_serial.arms[arm].primary_curve["equity"].reset_index(drop=True),
            res_par.arms[arm].primary_curve["equity"].reset_index(drop=True),
            check_exact=True,
        )
        print(f"OK: arm={arm} serial == parallel (bit-exact)")
    print("OK: serial == parallel for all arms (bit-exact)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 跑全套测试**

Run: `.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 615+ PASS,无新 FAIL

- [ ] **Step 3: Commit**

```bash
git add src/stockpool/portfolio_ab/runner.py src/stockpool/cli.py tests/test_portfolio_ab_runner.py scripts/verify_parallel_arms.py
git commit -m "feat(portfolio-ab): parallel_arms opt-in (subprocess) + CLI flag + verify script"
```

### Task 4.5: 文档更新

**Files:**
- Modify: `CLAUDE.md`、`README.md`

- [ ] **Step 1: CLAUDE.md 加 `--parallel-arms` 说明**

在 portfolio-ab CLI 命令段加:
```
# Portfolio AB — opt-in 并行(两 arm 同时跑,内存 ~2×)
python -m stockpool portfolio-ab --config portfolio_ab.yaml --parallel-arms
```

- [ ] **Step 2: README.md 同步**

在 `常用命令` 段补 `--parallel-arms` 示例。

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md README.md
git commit -m "docs: note --parallel-arms flag on portfolio-ab"
```

---

## 收尾 — 端到端冒烟 + 总览

### Task 5.1: 全链路冒烟

- [ ] **Step 1: 跑全部测试**

Run: `.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 615+ PASS

- [ ] **Step 2: 跑全部 minimal example 校验脚本**

Run:
```bash
.venv/Scripts/python.exe scripts/verify_staggered_parallel.py
# 需要预备 portfolio_ab_small.yaml 才能跑 parallel_arms 校验;
# 如果时间紧可以省 — 已有 test_parallel_arms_matches_serial 等价覆盖
```
Expected: 各自 `OK:` 输出

- [ ] **Step 3: USE_PYTHON_OPS=1 回滚验证**

Run: `STOCKPOOL_USE_PYTHON_OPS=1 .venv/Scripts/python.exe -m pytest tests/test_ops_snapshot.py tests/test_wq101.py -q`
Expected: PASS — Rust 回滚开关可用

- [ ] **Step 4: 总结改动**

Run: `git log --oneline main..HEAD`
Expected: 看到 4 个 PR 的 commit 列表;每个 PR 都可独立 revert。

---

## Self-Review 笔记(写完已检)

- [x] 4 个 PR 各自有 minimal example + Rust 路径有 STOCKPOOL_USE_PYTHON_OPS=1 回滚
- [x] 类型一致:`StaggeredRunner` 多了 `components`,`run_portfolio_ab` 多了 `parallel_arms`,签名贯穿 cli + runner + ensemble
- [x] PR-T1.2 dispatcher 与已有 `rank` / `ts_std` 范式严格对齐
- [x] PR-T1.1 oracle 保留为 `_industry_neutralize_per_day_loop`,测试可对比
- [x] 每个 Task 末尾都有 commit 步骤
